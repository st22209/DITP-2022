def calc_area():
    length = float(input("What is the length of the rectangle in cm? "))
    width = float(input("What is the width of the rectangle in cm? "))
    print("The area is: {:.2f} cm2".format(width*length))

calc_area()